/*
The conditional or ternary operator in JavaScript is a great way to shorten if/else statements.  It can
be written on a single line (see below).
*/

function checkEqual(a, b) {
    return a === b ? true : false;
}

checkEqual(1, 2);