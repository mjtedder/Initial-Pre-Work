// Since comparison operators return a boolean value of true or false, instead of using an if/else statement
// to evaluate these values, we can just write return (a === b); to achieve the same result in less code.

function isLess(a, b) {
    // Fix this code
    return (a < b);
}

// Change these values to test
isLess(10, 15);