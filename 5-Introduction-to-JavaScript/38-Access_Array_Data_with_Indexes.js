/*

We can use bracket notation to access the data inside arrays.  It also uses zero-based indexing, which means
the first piece of data in the array is accessed by using myArray[0]

*/

// Example
var ourArray = [50,60,70];
var ourData = ourArray[0]; // equals 50

// Setup
var myArray = [50,60,70];

// Only change code below this line.
var myData = myArray[0];