// We can access property values in a JavaScript object using dot notation(.) or brack notation similar to when we
// access values in an array ([]).  Below we accessed the testObj property values with dot notation.

// Setup
var testObj = {
    "hat": "ballcap",
    "shirt": "jersey",
    "shoes": "cleats"
};

// Only change code below this line
var hatValue = testObj.hat;
var shirtValue = testObj.shirt;