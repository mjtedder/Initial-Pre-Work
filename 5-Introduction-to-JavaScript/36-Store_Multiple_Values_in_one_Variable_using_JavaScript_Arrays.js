/*

We can use an array in JavaScript to store multiple data types in one place.  To do this, start an array declaration
with an opening bracket ([), insert the data separated by commas(,) then close the array with a closing bracket(]).

*/

// Example
var ourArray = ["John", 23];

// Only change code below this line.
var myArray = ["Matt", 32];