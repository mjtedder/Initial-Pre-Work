/*

Sometimes we want to have a literal quote inside of a string.  To prevent JavaScript from reading the double quotes
as the end of the string we a declaring, we use the \ symbol.  This tells JavaScript to ignore the quotation marks
and print the message to the console or to the browser.

*/

var myStr = "I am a \"double quoted\" string inside \"double quotes\".";