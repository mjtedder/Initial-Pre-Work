/*

Another data type in JavaScript is called booleans.  Booleans only have two values, true or false.  They are not written
with quotes.  If you were to declare a variable with "true" or "false", the data type would be a string, not a boolean.

*/

function welcomeToBooleans() {

    // Only change code below this line.

    return true; // Change this line

    // Only change code above this line.
}