// We can use the Math.random() function in JavaScript to generate a random number between 0 and not quite 1.  There
// a way to generate whole numbers we will explore in future lessons.

function randomFraction() {
    // Only change code below this line.
    return Math.random();
    // Only change code above this line.
}