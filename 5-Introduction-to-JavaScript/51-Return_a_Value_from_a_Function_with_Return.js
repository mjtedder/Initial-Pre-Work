/*

We can pass values into a function using arguments.  To get that value back, we use the keyword return to send a value
back out of a function.

*/

// Example
function minusSeven(num) {
    return num - 7;
}

// Only change code below this line
function timesFive(num) {
    return num * 5;
}

console.log(timesFive(10));