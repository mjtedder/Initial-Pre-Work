/*
We can use the parseInt() JavaScript function to turn a string into an integer.  If the first character of the string
cannot be turned into an integer, it will return NaN (Not a Number).
*/

function convertToInteger(str) {
    return parseInt(str);
}

convertToInteger("56");